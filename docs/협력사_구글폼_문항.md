# 협력사 입력 구글폼 문항 (T21)

**작성** 2026-09-12 ｜ **연동** `scripts/google_form_sync.gs` → Supabase `partners`

협력사가 채우는 폼이다. 목표 소요 **3분 이내**.

---

## 읽는 법

각 문항을 아래 세 가지로 나눠 적었다.

| | 구글폼에서 |
|---|---|
| **질문** | 문항 제목 칸에 넣는다 |
| **설명** | 「설명」을 켜서 그 칸에 넣는다. 안 넣어도 되고 고쳐도 된다 |
| **형식** | 단답형 / 장문형 / 객관식 중 무엇인지 |

**`[일치필요]` 가 붙은 질문은 글자 하나까지 그대로 써야 한다.**
연동 스크립트가 제목으로 답을 찾기 때문이다. 어긋나면 오류가 나지 않고
**그 항목만 비어서 저장된다.** 대신 Apps Script 실행 기록에 못 찾은 제목이
남으므로, 첫 제출 뒤 한 번 확인하면 바로 알 수 있다.

설명과 예시는 자유롭게 바꿔도 된다. 제목만 맞으면 된다.

> 아래 `## 1.` `## 2.` 는 **이 문서의 절 번호**이지 문항 제목이 아니다.
> 폼 제목에 번호를 붙이면(`1. 확인 코드`) 스크립트가 못 찾는다.

> **개인정보를 묻지 않는다.** 연락처·주소는 이미 알고 있어 받을 이유가 없고,
> 받아 두면 그 정보가 개발자 개인 구글 계정에 쌓인다.

---

## 0. 폼 설정

| | |
|---|---|
| 응답 수정 | **허용** — 나중에 가격이 바뀌면 같은 링크로 고친다 |
| 로그인 요구 | **안 함** — 구글 계정이 없을 수 있다 |
| 이메일 수집 | **안 함** |

보낼 때는 **확인 코드를 미리 채운 링크**를 만들어 보낸다.
(폼 편집 화면 우측 상단 ⋮ → 「미리 채워진 링크 가져오기」 → 확인 코드 칸에
그 협력사의 `invite_code` 를 넣고 링크 복사)

---

## 1. 확인 코드

**질문** `확인 코드` **[일치필요]**
**설명** 바틀링에서 보내드린 링크에 이미 채워져 있습니다. 고치지 말아주세요.
**형식** 단답형 · 필수

> 이 값으로 어느 협력사의 답인지 찾는다. 비어 있거나 바뀌면 연동이 안 된다.

---

## 2. 가게

**질문** `가게 이름` **[일치필요]**
**설명** (없음)
**형식** 단답형 · 필수

---

**질문** `업종` **[일치필요]**
**설명** (없음)
**형식** 객관식 · 필수

**선택지 값도 글자 그대로 써야 한다 [일치필요]**

```
제과·디저트
카페
분식·스낵
피자·튀김·그릴
한식
중식
일식
양식
패스트푸드
```

> 고른 값이 `partners.category` 에 그대로 들어간다. `context/builder.py` 의
> `INDUSTRY_MAP` 키와 같아야 그 업종의 상권 매출을 붙일 수 있다 (명세서 3-2).
> 목록에 없는 업종이 들어오면 매출 데이터 없이 기획이 돌아간다.

---

## 3. 지금 팔고 계신 메뉴

**섹션 설명** 바틀링이 사 와서 파는 형태라, 지금 팔고 계신 것 그대로면 됩니다.
새로 만드실 필요는 없습니다.

메뉴마다 세 칸을 따로 묻는다. 한 칸에 몰아 받으면 사람마다 적는 방식이 달라
옮겨 담을 때 깨진다.

### 메뉴 1

**질문** `메뉴 1` **[일치필요]**
**설명** 예: 붕어빵 3개
**형식** 단답형 · 필수

**질문** `메뉴 1 가격` **[일치필요]**
**설명** 손님에게 받으시는 값입니다. 숫자만 적어주세요. 예: 3000
**형식** 단답형 · 필수 · 응답 확인 `^[0-9,]+$`

**질문** `메뉴 1 납품가` **[일치필요]**
**설명** 바틀링에 주실 수 있는 값입니다. 정하지 못하셨으면 비워두셔도 됩니다.
**형식** 단답형 · 응답 확인 `^[0-9,]*$`

### 메뉴 2 ~ 메뉴 5

메뉴 1과 똑같은 세 칸을 숫자만 바꿔 만든다. **[일치필요]**

```
메뉴 2      메뉴 2 가격      메뉴 2 납품가
메뉴 3      메뉴 3 가격      메뉴 3 납품가
메뉴 4      메뉴 4 가격      메뉴 4 납품가
메뉴 5      메뉴 5 가격      메뉴 5 납품가
```

메뉴 1~3은 필수, 메뉴 4~5는 선택으로 둔다.

> **띄어쓰기까지 같아야 한다.** 스크립트가 `메뉴 ` 뒤에 숫자를 붙여 찾는다.
> `메뉴1` 이나 `메뉴 1번` 은 못 찾는다.

> **납품가를 메뉴마다 받는 이유.** 메뉴마다 원가가 다르다. 매장에 단가
> 하나만 두면 어떤 메뉴에서는 소매가보다 비싸게 매입하는 값이 나온다.
> 선택 항목이라 안 적으시면 그 메뉴의 매입가는 협의로 정한다.

> **가격 칸에 응답 확인을 거는 이유.** "3000원"이나 "삼천원"이 들어오면
> 숫자로 바꿀 수 없다.

---

**질문** `그중 대표 메뉴` **[일치필요]**
**설명** 가장 많이 나가는 것 하나를 적어주세요.
**형식** 단답형

---

## 4. 일정

**질문** `납품 가능한 요일과 시간` **[일치필요]**
**설명** 예: 화~일 오전 중 가능합니다. 월요일은 휴무입니다
**형식** 단답형 · 필수

> 실행일을 정하는 데 쓰인다. 만드는 날과 가져다주는 날이 맞아야 하므로,
> 어느 요일 어느 시간대에 납품이 가능한지를 묻는다.
>
> 「절대 불가 조건」과 겹쳐 보이지만 층이 다르다. 이쪽은 평소 되는
> 범위이고, 저쪽은 어떤 경우에도 안 되는 것이다.

---

**질문** `협의 가능한 시간` **[일치필요]**
**설명** 전화나 방문으로 이야기 나누기 편하신 때를 적어주세요.
예: 평일 오전 / 브레이크타임 15~17시
**형식** 단답형 · 필수

> 납품 가능 시간과 다르다. 물건을 가져다주실 수 있는 때와 전화를 받으실
> 수 있는 때가 다르다. 매입가와 납품 수량은 결국 만나서 정해야 하므로
> 이 값이 있어야 약속을 잡을 수 있다.

---

## 5. SNS

**섹션 설명** 바틀링과 같이 올리면 같은 노력으로 두 배가 닿습니다.
적어주시면 홍보 기획에 반영합니다.

**질문** `SNS 채널` **[일치필요]**
**설명** 예: 인스타그램
**형식** 단답형

---

**질문** `팔로워 수` **[일치필요]**
**설명** 대략이면 됩니다.
**형식** 단답형 · 응답 확인 `^[0-9,]*$`

---

**질문** `주로 올리시는 것` **[일치필요]**
**설명** (없음)
**형식** 객관식 — 릴스 / 피드 / 스토리

---

## 6. 절대 불가 조건

**질문** `이것만은 안 된다는 것` **[일치필요]**
**설명** 한 줄에 하나씩 적어주세요. 여기 적어주신 것을 어기는 기획은
만들어지지 않습니다.
**형식** 장문형 · 필수

**예시 (placeholder)**

```
주말은 자체 매장 운영으로 어렵습니다
반죽은 당일 소진해야 합니다
1주 전에는 알려주셔야 합니다
```

> 가장 중요한 문항이다. 하드 제약으로 쓰여 이것을 어긴 안은 폐기된다.

---

## 일치필요 문항 한눈에

| 질문 제목 | 들어가는 곳 |
|---|---|
| `확인 코드` | 어느 협력사인지 찾는 열쇠 |
| `가게 이름` | `name` |
| `업종` (선택지 값 포함) | `category` |
| `메뉴 1` ~ `메뉴 5` | `menu_prices[].메뉴` |
| `메뉴 1 가격` ~ `메뉴 5 가격` | `menu_prices[].가격` |
| `메뉴 1 납품가` ~ `메뉴 5 납품가` | `menu_prices[].납품가` |
| `그중 대표 메뉴` | `signature_menu` |
| `납품 가능한 요일과 시간` | `available_slots` |
| `협의 가능한 시간` | `contact_slots` |
| `SNS 채널` | `sns_channel` |
| `팔로워 수` | `sns_followers` |
| `주로 올리시는 것` | `sns_content_type` |
| `이것만은 안 된다는 것` | `blockers` |

---

## 묻지 않는 것과 그 이유

| 항목 | 왜 뺐나 |
|---|---|
| 보유 식재료 | 완성품을 사 오므로 무엇으로 만드시는지는 우리 일이 아니다 |
| 협업 가능 형태 | 완제품 매입 하나로 정해져(기획서 6-1) 고를 것이 없다 |
| 연락처·주소 | 이미 알고 있다. 받아 두면 개인 구글 계정에 남는다 |
